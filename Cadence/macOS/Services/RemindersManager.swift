// RemindersManager moved to Services/CadenceRemindersManager.swift so iOS and macOS use the same
// EventKit reminders store — nothing in it was ever AppKit-bound. (Prefixed file, unprefixed type,
// exactly like Shared/CadenceCalendarVisibilityPreferences.swift; this tombstone owns the old name.)
