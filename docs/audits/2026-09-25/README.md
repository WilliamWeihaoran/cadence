# Queue Research Handoff

```text
Tree read: c8735e4
Dirty files: 23 at this batch's capture; committed snapshot used
Close-out HEAD: 7b5897d; new MCP constructor/dispatch delta inspected separately
Mode: documentation-only edits, source/history inventories and primary-source web research
No product/test/TODO edits; no builds, tests, app launches, or device benchmarks
R61 was completed in the preceding turn and has its own capture metadata
```

All 13 previously unanswered non-standing request sections now have inline responses in
`docs/CODEX_REQUESTS.md`. **Answered is not the same as runtime-certified or coordinator-folded.**
Fold markers were preserved. R32-R38 answers sit below the historical R54 fold boundary, so the
inbox's new-answer list does not surface them; use the first report below for those reconciliations.

| Requests | Report | Most useful outcome |
| --- | --- | --- |
| R32-R35, R37-R38 | [Queue reconciliation](queue-reconciliation.md) | Updated counts and closures; no duplicate tickets for already repaired first-list, grouping, routing or hit-testing work. |
| R55-R56 | [Naming and asset boundary](naming-screen.md) | All eight new names conflicted; selected folded-D artwork is missing, so no invented render clearance. |
| R57, R60 | [Accessibility and SDK](accessibility-sdk.md) | A staged text-scaling plan; exact distinction between documented contracts and unmeasured framework observations. |
| R58, R59, R62 | [MCP, widgets, startup](mcp-widget-startup.md) | Store-wide enabled MCP authority; real widget failure paths; remaining startup measurement targets. |
| R61, preceding turn | [Recent fix claims](recent-fix-claims.md) | Three focused coverage/oracle recommendations; no new demonstrated destructive regression. |
| R33/R37/R38/R57 data | [Generated inventory](queue-inventory.json), [reader](queue_inventory.rb) | Full lexical history differences, repeated-ID candidates, native-view declarations, font counts and spacing candidates. |

## Recommended Order

1. Decide the supported MCP trust model before expanding write authority further: trusted host
   scoping/approval, bulk previews, recovery expectations. Current close-out has 16 write tools.
2. Take R61's focused test gaps and keep T-1318's product invariants strict. In-flight test edits
   were excluded, so reconcile with their owner before implementing another copy.
3. Build text scaling as a complete task workflow, not a blind font replacement. Verify the
   keyboard and narrow-pane cases before making an App Store accessibility support claim.
4. Instrument widgets and cold startup before optimizing or caching. T-1314/T-1341 already paid
   for the obvious tag/index and no-legacy-migration improvements.
5. Consume old-request premise corrections; do not reopen parked CloudKit orphan collection or
   calendar-ID decisions on the strength of old reports.

## Explicit Evidence Still Needed

| Item | Why not certified | Next concrete action |
| --- | --- | --- |
| R32 manifest behavioral equivalence | Only source/name inventory run | Run existing classifier/hygiene suite in an authorized test batch, not a new approximate classifier. |
| R33 exhaustive unrelated-work semantics | Full candidate set is lexical; repeated references can be legitimate | Review a bounded candidate subset if another historical collision must be proved; existing guards already prevent the original future allocation shape. |
| R34 exact click minima | No app walkthrough; selection/permissions change counts | Full-screen clean-fixture walkthrough with starting state and input method recorded. |
| R38 every visual grouping state | Source candidates do not prove every conditional composed/painted gap | Choose surface/state coverage, then full-screen screenshots and family-level composition checks. |
| R55 selected artwork | Only README exists in referenced directory | Restore/supply concept A; then actual-size light/dark raster QA. Geometry exploration is superseded by R56. |
| R57 accessibility behavior | Design/source risk, not a rendered or VoiceOver audit | Three-device largest-size workflow tests plus actual assistive navigation. |
| R58 distribution boundary | Project settings are not signed archive evidence | Inspect release packaging, helper signature/entitlements and launcher policy. |
| R59 budget headroom | No cold-device telemetry | Measure generation stages, peak memory and termination/freshness behavior. |
| R60 exact 26/27 differences | Notes do not specify observed values | Same commit, known runtime/toolchain matrix, scoped sentinel observations. |
| R62 cold startup cost | Prior measurements are in-memory maintenance | Disk-backed cohorts and stage/first-frame timing, separately varying bytes and fanout. |

These are bounded remaining verification tasks, not silently completed work or new severity claims.
Standing R6/R11/R12/R31/R45/R50 remain recurring by design; they are not one-shot unanswered tickets.

## Validation

- Generated inventory executed successfully against the archived source and c8735e4 history.
- `scripts/codex-inbox.sh` reports no non-standing unanswered IDs after the inline responses.
- Document source paths/line bounds and relative artifact links checked; no claim that these checks
  validate runtime behavior.
- Commit-read freshness is explicit: 7b5897d added two guarded MCP constructors during writing.
  Historical counts remain tied to c8735e4; they are not silently presented as 7b5897d counts.
