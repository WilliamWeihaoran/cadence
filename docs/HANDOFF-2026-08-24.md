# Interrupted-work record — 2026-08-24 14:48 (+0800)

Captured read-only. **Nothing was resumed, committed, killed or cleaned up** when this was
written — the user asked for a record only.

HEAD `e2af4e9`, **0 unpushed commits**, 66 uncommitted paths.

## How this happened

Fifteen agents were running when the 5-hour usage cap hit at ~14:40. Eleven died mid-work in the
same minute. **Their code survives in the working tree** — that is the normal outcome here, and the
tree below is recovery material, not damage.

## Where each workstream stopped

Ordered by how close it is to done. Quoted text is the agent's own last line.

| Ticket | Stopped at | Left to do |
|---|---|---|
| **T-20** macOS Settings | *"Now the tests."* | Implementation appears complete; only verification remains |
| **T-19** data safety / export | *"Tree is isolated and HEAD-restored. Now the lock, build, and test in a single call."* | Verification only |
| **T-214** iOS list completion | *"Baseline failed to compile — one bad `#expect` in my new test (a `rethrows` call inside the macro). Fixing it."* | One test compile error, then verify |
| **T-195b** sections-due-today | *"macOS: 1 failure — a live invariant test on host count that my nested host trips."* | **One real test failure** to resolve honestly, then verify |
| **T-251** Calendar Board rails | *"Now the full run — lock, three schemes, nine mutations, all in one invocation."* | Full verification + 9 mutations. **Also: main-actor warning at `CadenceRegularPaneLayout.swift:793` — baseline is 0** |
| **T-271** Goals narrow route | *"The probe re-run is queued behind agent `capsule`. Waiting."* | `NSHostingView` probe + 4 mutations |
| **capsule** (R6 count badge) | *"The render comparison was flaky (byte-equality on macOS Button chrome). Rewriting it as a coarse, deterministic ink statistic."* | Finish the deterministic test, then both mutations |
| **T-161** tests-pin-wiring audit | *"Hit T-236 contention (two host PIDs, 1542 failures, 0 compile errors). Discarding and re-running."* | Re-run; the discarded result was contention, not a regression |
| **T-22** App Review audit | (no final line; had stalled once on a Monitor notification) | The claim-by-claim table is the deliverable |
| **T-266** iOS focus entry | *"I'll start by reading the required docs and finding the predecessor's work."* | Barely started, but **its predecessor's code is in the tree** |
| **T-272** Goals timeline attach | *"I'll start by reading the required docs and the ticket."* | Barely started |

## Finished but NOT committed

- **T-123** repo-tightening audit. Fixed two stale counts in `AGENTS.md` (iOS 93→**96**, tests
  177→**186**); left the same counts stale in `CLAUDE.md` and `Cadence/iOS/AGENTS.md` deliberately,
  because live agents were rewriting both. Filed **T-275**: `SectionEyebrowLabel` is hand-rolled at
  **9 sites** with kerning drifted independently (0, 0.6, 0.7, 0.8 vs the component's 0.8) and one
  site using `Theme.muted` instead of `Theme.dim`. Not fixed — it is a visible macOS change needing
  screenshots. Also confirmed T-123 item 3 (MCP) is already closed by inspection, and that `Shared/`
  has grown **+65% in lines** since the ticket was filed, so item 1 is measurably progressing.

## Decided by the user, not yet started

**T-18 → "fix the two date defects only".** Both are wrong *today* for European users, independent
of any localisation:

1. `DateFormatters.longDate` / `shortDate` / `fullShortDate` / `dayOfWeek` / `monthAbbrev` are the
   only fixed-format formatters **not** locale-pinned, while `monthYear`'s own doc comment states
   pinning as the repo rule. On `zh_Hans_CN`, `"EEEE, MMMM d"` renders `星期一, 八月 24`.
2. `MonthCalendarPanel` (`Shared/Components/CadenceDatePicker.swift:93,190`) hardcodes
   `["Su","Mo",…]` and derives leading blanks from `weekday - 1`, i.e. Sunday-first unconditionally.
   Its sibling `CadenceScheduleSupport.weekdaySymbols` rotates by `firstWeekday`, and its doc records
   that skew shipping in Germany and Saudi Arabia. **The two shared month grids already disagree in
   Monday-first regions.**

The rest of T-18 stays backlogged. **T-17** likewise: axis decided (more iPhone/iPad sizes; *not* a
lower OS floor, *not* a new platform family), backlogged until the user names devices.

## Attribution of the 66 uncommitted paths (inferred from file names — verify before committing)

- **T-19 / T-22**: `CadenceDataExportService.swift`, `CadenceDataExportPresentation.swift`,
  `iOSDataExportSettingsSection.swift`, `CadenceDataExportSurfaceTests.swift`,
  `SettingsDataSafetySection.swift`, `docs/{app-review-notes.md,apple-release-readiness.md,privacy.html,support.html}`
- **T-266**: `CadenceFocusHandoff.swift`, `CadenceFocusBundleSupport.swift`, `iOSFocusView.swift`,
  `iOSCalendarBundleDetailSheet.swift`, `iOSTaskRowActionViews.swift`, `iOSRootView.swift`,
  `CadenceApp.swift`, `FocusPickerPlayControlTests.swift`
- **T-195b**: `CadenceTodayOverdueSummarySupport.swift`, `CadenceTodayOverdueSummaryCards.swift`,
  `CadenceTodayOverdueSummarySurfaceTests.swift`, `TasksPanel*.swift`, `iOSTodayTaskSections.swift`,
  `iPadToday*.swift`
- **T-214**: `iOSListWindDownSupport.swift` + `CadenceListWindDownSurfaceTests.swift` (new), with
  `iOSListArchiveSupport.swift` and `CadenceListArchiveSurfaceTests.swift` **deleted** — an
  archive→wind-down rename. Plus `CadenceTaskContainerLifecycleService.swift`, `iOSListViews.swift`,
  `iOSListsRegularPane.swift`, `iOSListDetailView.swift`, `iOSSettingsView.swift`
- **T-251 + T-271** (shared file — hunks must be separated): `CadenceRegularPaneLayout.swift`,
  `CadencePaneWidthRuleHomesTests.swift`, `CadenceRegularPaneLayoutTests.swift`,
  `CadenceDesktopSplitLayoutTests.swift`; T-251 also `CalendarBoard*/CalendarPageBoard*`; T-271 also
  `GoalsView.swift`, `GoalInspectorView.swift`
- **T-272**: `CadenceGoalTimelineRouteTests.swift`, `GoalTimeline*.swift`
- **capsule**: `CadenceTaskGroupHeading.swift`, `ListDetailSupportViews.swift`,
  `CadenceInboxRemindersSurfaceTests.swift`
- **Unattributed, needs checking before commit**: `Cadence.xcodeproj/project.pbxproj`,
  `Cadence/Info.plist`, `KanbanBoardSupport.swift`, `iOSListSupportViews.swift`,
  `NotificationSchedulingTests.swift`. **The project-file change matters** — this repo uses
  file-system-synchronized groups, so new `.swift` files should NOT require a `pbxproj` edit.

## Machine state at capture

0 real `xcodebuild`; lock free; **1 orphaned test host and 1 orphaned `CadenceMCPServer`** (no build
running, so both are strays); 1 booted simulator; 129G free. Eleven per-agent scratch directories
totalling ~23G, each holding an isolated tree and DerivedData — several contain finished build logs
worth reading before any re-run.

## Known hazards for whoever picks this up

- **Two commits landed earlier without mutation evidence** (`2f018a4`, `88a88d9`). A verifier proved
  `2f018a4`'s count-capsule half is only *partially* pinned: mutating `CadenceTaskGroupHeading:60`
  to `countBadge(count ?? 0)` restores the visible bug and all 2410 tests stay green. That is what
  the **capsule** agent was fixing.
- Verification numbers from a run showing a four-figure failure count with 0 compile errors and two
  test-host PIDs are **contention (T-236)** — discard, do not debug.
- `af03fb1` unintentionally swept another agent's `AGENTS.md` edit (the T-179 `detach` mitigation)
  into a T-117-titled commit. Content is correct; attribution in the message is not.
